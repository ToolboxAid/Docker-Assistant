#!/bin/bash

if [[ $EUID -ne 0 ]]; then
   echo "This script must be run as root"
   exit 1
fi

export $(grep -v '^#' .env | xargs -d '\n')
printenv

docker-compose up -d

docker ps

echo "delete below this line for production"
docker-compose logs -f

docker ps

